# دليل إعداد التحديث التلقائي لـ GitHub Gist

## الخطوة 1: إنشاء Personal Access Token

1. اذهب إلى: https://github.com/settings/tokens?type=beta
2. اضغط **Generate new token**
3. أعطه اسم مثل: `revoked-licenses-updater`
4. في **Repository access** اختر: `Public Repositories (read-only)`
5. في **Account permissions** > **Gists** اختر: `Read and write`
6. اضغط **Generate token**
7. **انسخ التوكن فوراً** (لن يظهر مرة أخرى!)

## الخطوة 2: إنشاء الـ Gist

1. اذهب إلى: https://gist.github.com
2. في اسم الملف اكتب: `revoked-licenses.json`
3. في المحتوى اكتب:
```json
{
  "version": 1,
  "updatedAt": "2024-01-01T00:00:00.000Z",
  "revokedKeys": []
}
```
4. اضغط **Create public gist**
5. انسخ **Gist ID** من الرابط:
   - الرابط: `https://gist.github.com/username/abc123def456`
   - الـ ID هو: `abc123def456`

## الخطوة 3: تعديل السكريبت

افتح ملف `scripts/update-revoked-gist.ps1` وعدّل:

```powershell
$GITHUB_TOKEN = "ghp_xxxxxxxxxxxx"  # التوكن الذي نسخته
$GIST_ID = "abc123def456"           # معرف الـ Gist
```

## الخطوة 4: الاستخدام

### طريقة 1: من صفحة الأدمن
1. ألغِ التراخيص المطلوبة
2. اضغط **📥 تصدير الملغاة**
3. احفظ الملف باسم `revoked-licenses.json` في مجلد `scripts`
4. شغّل السكريبت

### طريقة 2: من CMD
```cmd
cd scripts
powershell -ExecutionPolicy Bypass -File update-revoked-gist.ps1
```

### طريقة 3: اختصار (Shortcut)
1. انقر يمين على `update-revoked-gist.ps1`
2. اختر **Create shortcut**
3. انقل الاختصار لسطح المكتب
4. بعد كل تصدير، فقط اضغط على الاختصار

## رابط الملف للتطبيق

بعد أول تحديث ناجح، انسخ الرابط الذي يظهر (يبدأ بـ `https://gist.githubusercontent.com/...`)
والصقه في إعدادات التزامن بالتطبيق.

## ملاحظات أمنية

- لا تشارك التوكن مع أحد
- التوكن يعطي صلاحية تعديل الـ Gists فقط
- يمكنك حذف التوكن وإنشاء واحد جديد في أي وقت
